import { Typography, Container } from '@mui/material'; 
export default function Admin() {
  return (
    <Container maxWidth={false} sx={{mt:15}}>
      <Typography variant="h2">Manage Phone Numbers</Typography>
    </Container>
    );
}